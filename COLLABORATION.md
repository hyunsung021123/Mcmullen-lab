# COLLABORATION.md — 다자간 AI 협업 프로토콜 (Claude Code · Codex · ChatGPT)

이 문서는 이 저장소에서 **서로 다른 세 AI(Claude Code, Codex, ChatGPT)** 가 같은
코드베이스를 비동기로, 그리고 **서로 다른 접근 권한**으로 협업할 때 지켜야 할 규약을
정의합니다.

## 0. 전제 1: 우리는 실시간으로 연결돼 있지 않다

세 AI는 서로 다른 세션에서, 서로 다른 시점에 실행됩니다. 직접적인 실시간 채널은
없습니다. **따라서 이 저장소(커밋, PR, 이슈, 아래의 결정 로그)가 세 참여자 사이의
유일한 공유 기억입니다.** 어떤 변경이든, "이 저장소 상태만 보고" 다른 쪽이 무엇이 왜
바뀌었는지 완전히 재구성할 수 있어야 합니다. 말로만 설명하고 저장소에 남기지 않은
맥락은 존재하지 않는 것과 같습니다.

## 0-1. 전제 2: 세 참여자의 저장소 접근 권한은 서로 다르다

이게 2자 구도와의 핵심 차이입니다 — 권한이 대칭이 아닙니다.

| 참여자 | 저장소 접근 방식 | 기본 역할 |
|---|---|---|
| **Claude Code** | 이 저장소에 **직접** 커밋/푸시 (Git 자격증명 보유) | 최초 구현, 버그 수정, 복잡한 리팩터링 |
| **Codex** | 사용자의 **로컬 폴더(clone)를 통해 간접적으로** 수정 → 최종적으로 `git push`로 반영 | 독립 구현, 테스트·벤치마크, 반복 수정, 대안 구현 |
| **ChatGPT** | 저장소 **읽기 전용** — 커밋/PR/이슈를 직접 작성할 수 없음 | 저장소·PR·CI 기반 리뷰, 설계 및 수학적 일관성 검토, 현황을 사용자에게 설명 |
| **사람(관리자)** | 저장소 admin | 작업 배정, 충돌 해결, 최종 병합, 저장소 설정(branch protection 등) |

역할 이름은 고정되어 있지 않고 상황에 따라 바뀔 수 있지만(예: Claude가 리뷰를, Codex가
최초 구현을 맡을 수도 있음), **접근 권한의 비대칭성은 그때그때 실제 설정을 따른다** —
이 표가 최신 상태를 반영하지 못하면 이 문서부터 고친다.

> **절차는 여기 담지 않는다.** 브랜치 이름 규약, 작업 시작/완료 체크리스트, 인수인계
> (HANDOFF) 형식 등 "어떻게"에 해당하는 실무 절차는 `docs/AI_WORKFLOW.md`에 있다. 이
> 문서(`COLLABORATION.md`)는 "왜/무엇"에 해당하는 원칙만 담는다.

### 이 비대칭이 낳는 실무적 결과

- **ChatGPT는 §3의 채널을 직접 쓸 수 없다.** ChatGPT가 낸 의견/반박은 반드시
  **사용자가 대신** GitHub 이슈나 PR 코멘트로 옮겨야 저장소에 반영된다. 이때 출처를
  `(source: chatgpt, relayed by user)`처럼 명시한다 — 안 그러면 나중에 "누가 이 지적을
  했는지" 추적이 안 된다.
- **Codex의 커밋은 Codex 고유의 GitHub 계정으로 안 남을 수 있다.** 로컬 폴더 경유이기
  때문에 커밋 author가 사용자 본인 계정으로 찍힐 가능성이 높다. 그러므로 "누가
  커밋했는가"(git author)만으로는 역할을 구분할 수 없고, **PR/커밋 메시지의 역할
  체크박스**(아래 §3.1, PR 템플릿)가 실제 역할을 알 수 있는 유일한 단서가 된다.
- **Claude Code는 주기적으로 저장소 상태(협업자 목록·브랜치·커밋 이력·PR)를 조회해
  이 표와 실제 설정이 맞는지 점검할 수 있다.** 단, Codex의 로컬 폴더 내부나 ChatGPT의
  읽기 연동 자체는 저장소 바깥에 있어 Claude Code가 볼 수 없는 구조적 사각지대다 —
  이건 "연동이 잘못됐다"는 신호가 아니라 원래 확인 불가능한 영역이니 혼동하지 말 것.

## 1. 최우선 순위: CLAUDE.md의 불변 조건

`CLAUDE.md`에 명시된 신뢰 모델(불변 조건)은 **세 참여자 모두에게 최우선으로 구속력을
가진다.** 특히:
- `om_core.py`가 유일한 수학적 진실이라는 것,
- `theorist.py`의 반박자(`counterexample_hunter`, `proof_checker`)가 항상 결정론적 코드여야
  한다는 것,
- `acyclic`/`totally_cyclic`/`convex_position`을 섞지 않는다는 것,
- `criteria.py`의 `REGISTRY`에 없는 이름을 쓰지 않는다는 것

— 이 네 가지는 "최적화"나 "리팩터링"을 이유로도 깨뜨릴 수 없다. Codex나 ChatGPT가 이
조건을 위반하는 제안을 하면, 그 제안을 반드시 기각하고 사유를 아래 결정 로그에 남긴다.

## 2. 모든 변경은 아래 네 채널 중 하나로 반드시 드러나야 한다

### 2.1 커밋 / PR 메시지
모든 커밋(특히 PR)은 다음을 포함한다:
- 무엇을 바꿨는지
- 왜 바꿨는지 (동기/문제)
- **누가 제안/실행했는지** — claude-code(직접) / codex(로컬 경유) / chatgpt(사용자 경유,
  직접 커밋 불가) / 사람 직접
- **영향 범위(blast radius)**: 이 변경이 `om_core.py`의 판별 로직, `criteria.py`의
  REGISTRY, `om_classes.py`의 클래스 레지스트리, 공개 함수 시그니처 중 무엇을 건드리는지
- 다른 참여자에게 남기는 질문(있다면)

`.github/PULL_REQUEST_TEMPLATE.md`가 이 형식을 강제한다.

### 2.2 `docs/DECISIONS.md` — 설계 결정 로그
diff만으로는 "왜 이 대안을 택했는가"가 드러나지 않는다. 여러 설계 대안 중 하나를
고른 결정, 트레이드오프가 있는 결정, CLAUDE.md 불변 조건과 맞닿은 결정은 **반드시**
`docs/DECISIONS.md`에 append 방식으로 기록한다. 이 로그는 절대 과거 항목을 수정/삭제하지
않는다 — 새로운 결정으로 이전 결정을 뒤집을 때도 새 항목을 추가한다.

### 2.3 GitHub Issues — `cross-review` 라벨
다른 참여자에게 명시적으로 "이거 검토해줘, 반박해도 좋다"고 요청할 때는
`.github/ISSUE_TEMPLATE/cross_review.md` 템플릿으로 이슈를 연다. **ChatGPT는 이슈를 직접
열 수 없으므로**, ChatGPT의 검토 요청이나 의견은 사용자가 이 템플릿으로 대신 옮긴다.

### 2.4 CI (`ci.yml`) — 객관적 심판
사람의 판단이나 리뷰 합의와 무관하게, 모든 PR은 `CLAUDE.md`에 명시된 결정론적 자체
테스트(`python om_core.py`, `python criteria.py` 등)를 CI에서 통과해야 한다. 이건 누구의
"의견"도 아니고 검증 결과이므로 논쟁의 대상이 아니다. (저장소 관리자가 GitHub의 branch
protection에서 "머지 전 CI 통과 필수"를 켜는 것을 권장 — 이건 저장소 설정 변경이라
사람이 직접 해야 한다.)

## 3. 건설적 반박의 형식

누구든 다른 참여자의 설계에 반대할 때는 다음 세 가지를 반드시 포함한다 — 단순히
"이렇게 하지 마세요"는 반박으로 인정하지 않는다:

1. **정확히 무엇에 반대하는지** (파일:라인 또는 커밋/PR 인용)
2. **왜 문제인지** — 구체적인 실패 시나리오, 또는 명시적인 트레이드오프 설명
   (막연히 "안 좋다"가 아니라 "이 입력에서 이렇게 깨진다" 또는 "이 성능/가독성 비용이 든다")
3. **대안 제시**

이 반박은 PR 리뷰 코멘트로 남기거나, 설계 차원이면 `docs/DECISIONS.md`에 새 항목으로
추가한다. 원 제안자는 반박을 받아들이거나, 왜 받아들이지 않는지를 같은 채널에 남긴다 —
침묵으로 무시하지 않는다. ChatGPT발 반박은 사용자가 위 §2.3/§2.2 채널로 대신 기록한다.

## 4. 각 참여자 연동 시 권장 사항 (사람이 할 일)

- **최소 권한 원칙**: 쓰기 권한을 주는 경우(Codex 등) 이 저장소 하나에만 스코프된
  fine-grained PAT(또는 GitHub App)를 발급하고, 필요한 권한만 부여한다 (contents,
  pull requests, issues의 read/write 정도). 조직 설정이나 다른 저장소에 대한 권한은
  주지 않는다.
- ChatGPT는 읽기 전용이 이 프로젝트의 의도된 설계다 — 굳이 쓰기 권한을 추가로 줄
  필요는 없다. ChatGPT 쪽(Custom GPT Action 등)의 시스템 프롬프트에는 "작업 시작 전
  항상 `CLAUDE.md`와 `COLLABORATION.md`를 저장소에서 읽고 그 규약을 따르라"는 지시를
  명시적으로 넣는다 — Claude Code와 달리 ChatGPT/Codex는 이 파일들을 자동으로 읽지
  않는다.
- branch protection(머지 전 CI 필수, 리뷰 필수 등)은 GitHub 저장소 설정에서 사람이
  직접 켠다.

## 5. 관련 문서 지도

이 헌장은 원칙만 담고, 나머지는 아래 문서가 분담한다(중복 서술을 피하기 위함):

- `CLAUDE.md` — 수학적 불변 조건 + Claude 지침
- `AGENTS.md` — Codex/일반 에이전트 진입점(얇은 포워더)
- `docs/AI_WORKFLOW.md` — 실무 절차(브랜치·커밋·PR·HANDOFF·충돌 처리)
- `docs/WORKBOARD.md` — 활성 작업 인덱스
- `docs/RESEARCH_STATUS.md` — 현재 연구 목표/실험 상태
- `docs/DECISIONS.md` — append-only 설계 결정 로그
- `.github/ISSUE_TEMPLATE/ai_task.md` · `cross_review.md`, `.github/PULL_REQUEST_TEMPLATE.md`

## 6. 이 문서 자체도 예외가 아니다

이 협업 프로토콜을 바꾸는 결정(역할/권한 재분담, 채널 추가/제거 등)도 §2.2의 결정
로그에 기록한다. 최초 도입(2자 구도)은 `docs/DECISIONS.md`의 `0001`, 3자 비대칭 구조로의
수정은 `0002`, 브랜치/이슈/HANDOFF 기반 워크플로 정비는 `0003`, `develop`/`main` 2단계
승격 구조(루프는 사람 승인 없이, `main` 승격만 사람이 간헐적으로 승인)는 `0004` 항목에
있다.
