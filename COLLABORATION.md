# COLLABORATION.md — Claude Code ↔ ChatGPT 협업 프로토콜

이 문서는 이 저장소에서 **서로 다른 두 AI(Claude Code, ChatGPT)** 가 같은 코드베이스를
비동기로 협업할 때 지켜야 할 규약을 정의합니다.

## 0. 전제: 우리는 실시간으로 연결돼 있지 않다

Claude Code와 ChatGPT는 서로 다른 세션에서, 서로 다른 시점에 실행됩니다. 둘 사이에
직접적인 실시간 채널은 없습니다. **따라서 이 저장소(커밋, PR, 이슈, 아래의 결정 로그)가
두 AI 사이의 유일한 공유 기억입니다.** 어떤 변경이든, "이 저장소 상태만 보고" 다른 쪽이
무엇이 왜 바뀌었는지 완전히 재구성할 수 있어야 합니다. 말로만 설명하고 저장소에
남기지 않은 맥락은 존재하지 않는 것과 같습니다.

## 1. 역할 분담 (현재 합의된 시작점)

- **claude-code = 구현자(Implementer)**: 새 기능/모듈의 최초 구현, 버그 수정, 사용자가
  요청한 변경을 코드로 만든다.
- **chatgpt = 검토자/최적화(Reviewer & Optimizer)**: 이미 존재하는 코드의 최적화 제안,
  설계 검토, 성능/가독성/누락된 엣지케이스 지적, 대안 제시.

역할은 고정된 이름이 아니라 **기능**이다 — 상황에 따라 뒤바뀔 수 있고, 그때도 이 문서의
규약은 동일하게 적용된다. 즉 "claude-code가 검토자 역할을 맡는" 경우도 이 프로토콜을
그대로 따른다.

## 2. 최우선 순위: CLAUDE.md의 불변 조건

`CLAUDE.md`에 명시된 신뢰 모델(불변 조건)은 **두 AI 모두에게 최우선으로 구속력을 가진다.**
특히:
- `om_core.py`가 유일한 수학적 진실이라는 것,
- `theorist.py`의 반박자(`counterexample_hunter`, `proof_checker`)가 항상 결정론적 코드여야
  한다는 것,
- `acyclic`/`totally_cyclic`/`convex_position`을 섞지 않는다는 것,
- `criteria.py`의 `REGISTRY`에 없는 이름을 쓰지 않는다는 것

— 이 네 가지는 "최적화"나 "리팩터링"을 이유로도 깨뜨릴 수 없다. ChatGPT가 이 조건을
위반하는 최적화를 제안하면, claude-code(또는 사람)는 그 제안을 반드시 기각하고 사유를
아래 결정 로그에 남긴다. 반대도 동일하다.

## 3. 모든 변경은 아래 네 채널 중 하나로 반드시 드러나야 한다

### 3.1 커밋 / PR 메시지
모든 커밋(특히 PR)은 다음을 포함한다:
- 무엇을 바꿨는지
- 왜 바꿨는지 (동기/문제)
- **영향 범위(blast radius)**: 이 변경이 `om_core.py`의 판별 로직, `criteria.py`의
  REGISTRY, `om_classes.py`의 클래스 레지스트리, 공개 함수 시그니처 중 무엇을 건드리는지
- 상대방에게 남기는 질문(있다면)

`.github/PULL_REQUEST_TEMPLATE.md`가 이 형식을 강제한다.

### 3.2 `docs/DECISIONS.md` — 설계 결정 로그
diff만으로는 "왜 이 대안을 택했는가"가 드러나지 않는다. 여러 설계 대안 중 하나를
고른 결정, 트레이드오프가 있는 결정, CLAUDE.md 불변 조건과 맞닿은 결정은 **반드시**
`docs/DECISIONS.md`에 append 방식으로 기록한다. 이 로그는 절대 과거 항목을 수정/삭제하지
않는다 — 새로운 결정으로 이전 결정을 뒤집을 때도 새 항목을 추가한다.

### 3.3 GitHub Issues — `cross-review` 라벨
상대방에게 명시적으로 "이거 검토해줘, 반박해도 좋다"고 요청할 때는
`.github/ISSUE_TEMPLATE/cross_review.md` 템플릿으로 이슈를 연다. 일반적인 버그 리포트나
기능 요청과 구분하기 위함이다.

### 3.4 CI (`ci.yml`) — 객관적 심판
사람의 판단이나 리뷰 합의와 무관하게, 모든 PR은 `CLAUDE.md`에 명시된 결정론적 자체
테스트(`python om_core.py`, `python criteria.py` 등)를 CI에서 통과해야 한다. 이건 두 AI
누구의 "의견"도 아니고, 검증 결과이므로 논쟁의 대상이 아니다. (저장소 관리자가 GitHub의
branch protection에서 "머지 전 CI 통과 필수"를 켜는 것을 권장 — 이건 저장소 설정 변경이라
사람이 직접 해야 한다.)

## 4. 건설적 반박의 형식

검토자 역할(현재는 chatgpt)이 구현자의 설계에 반대할 때는 다음 세 가지를 반드시 포함한다
— 단순히 "이렇게 하지 마세요"는 반박으로 인정하지 않는다:

1. **정확히 무엇에 반대하는지** (파일:라인 또는 커밋/PR 인용)
2. **왜 문제인지** — 구체적인 실패 시나리오, 또는 명시적인 트레이드오프 설명
   (막연히 "안 좋다"가 아니라 "이 입력에서 이렇게 깨진다" 또는 "이 성능/가독성 비용이 든다")
3. **대안 제시**

이 반박은 PR 리뷰 코멘트로 남기거나, 설계 차원이면 `docs/DECISIONS.md`에 새 항목으로
추가한다. 구현자는 반박을 받아들이거나, 왜 받아들이지 않는지를 같은 채널에 남긴다 —
침묵으로 무시하지 않는다.

## 5. ChatGPT 쪽 설정 시 권장 사항 (사람이 할 일)

이 저장소가 ChatGPT와 아직 연동되지 않았다면, 연동 시:

- **최소 권한 원칙**: 이 저장소 하나에만 스코프된 fine-grained PAT(또는 GitHub App)를
  발급하고, 필요한 권한만 부여한다 (contents: read/write, pull requests: read/write,
  issues: read/write). 조직 설정이나 다른 저장소에 대한 권한은 주지 않는다.
- ChatGPT 쪽(Custom GPT Action 등)의 시스템 프롬프트에 "작업 시작 전 항상 `CLAUDE.md`와
  `COLLABORATION.md`를 저장소에서 읽고 그 규약을 따르라"는 지시를 명시적으로 넣는다 —
  Claude Code와 달리 ChatGPT는 이 파일들을 자동으로 읽지 않는다.
- branch protection(머지 전 CI 필수, 리뷰 필수 등)은 GitHub 저장소 설정에서 사람이
  직접 켠다.

## 6. 이 문서 자체도 예외가 아니다

이 협업 프로토콜을 바꾸는 결정(역할 재분담, 채널 추가/제거 등)도 §3.2의 결정 로그에
기록한다. 최초 도입 결정은 `docs/DECISIONS.md`의 `0001` 항목에 있다.
